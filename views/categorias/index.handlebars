<h1>Categorias</h1>
{{#each categorias}}
<div class="card">
    <div class="card-body">
        <a href="categorias/{{slug}}">
            <h3>{{nome}}</h3>
        </a>
    </div>
</div>
{{else}}

{{/each}}